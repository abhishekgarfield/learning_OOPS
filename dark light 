1) window.matchMedia("(prefers-color-scheme="dark)).matches // use system theme
2) document.body.classlist.toggle("dark-theme") // adding class to document body
3) if (theme.href=="light"){theme.setAttribute(href)="dark"} //changeing it to spearate sstylesheet